# Port Discovery
nmap -Pn -p23 <IP>
nmap -sV -sC -p23 <IP>

# NSE Scripts
nmap --script telnet-encryption -p23 <IP>
nmap --script telnet-brute -p23 <IP>
nmap --script telnet-* -p23 <IP>

# Banner Grabbing
nc <IP> 23
telnet <IP> 23

# Login
telnet <IP>

# Brute Force (Authorized Testing Only)
hydra -l root -P /usr/share/wordlists/rockyou.txt telnet://<IP>
hydra -L users.txt -P passwords.txt telnet://<IP>

# Search for Public Exploits
searchsploit telnet

# Metasploit
msfconsole
search telnet

# Once Logged In - Basic Enumeration
whoami
id
hostname
uname -a
ip a
ifconfig
netstat -tulnp
ss -tulnp
sudo -l
cat /etc/passwd
cat /etc/shadow
find / -perm -4000 -type f 2>/dev/null
find / -writable -type d 2>/dev/null
history
env

# Exit Session
exit
logout
Ctrl + ]
quit
